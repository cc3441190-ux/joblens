/**
 * 个人使用：复制本文件为 secrets.js，填入 Key（不要提交 secrets.js）。
 * 存在 secrets.js 时，侧栏可不填 API Key。
 * 分发场景：还可在扩展根目录配置 jobflow-default-api.js（见 jobflow-default-api.example.js），
 * 为没有自有 Key 的用户提供内置体验 Key（优先级：侧栏存储 > secrets.js > jobflow-default-api.js）。
 */
var LOCAL_DEEPSEEK_API_KEY = "";
var LOCAL_DEEPSEEK_MODEL = "";
